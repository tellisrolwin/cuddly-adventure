import React from "react";

export default function Des() {
  const obj = {
    name: "radha",
    age: 45,
    address: "mangalore",
  };
  const { name, age, address } = obj;
  return (
    <div>
      {d}
      {address}
    </div>
  );
}
